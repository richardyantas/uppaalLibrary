/*
  1.- probar con un lib mass pequenio y simple c++ 17, return 4
  1.- upaal recibe, la variable steado y recibirlo luego del query graphics .. just to verify! :) 
  1.- Install Jsoncpp, would be better locally
  2.- Make a library  libconfig.so for call function which fill the data in more organized way and more visually
*/

#include<iostream>
#include "config_wrapper.h"

int main()
{
  std::cout << get_num() << std::endl;
  return 0;
}